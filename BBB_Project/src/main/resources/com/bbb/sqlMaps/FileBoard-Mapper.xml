<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="FileBoard">

	<select id="getSeqNextvalue" resultType="int">
		select bNum_seq.nextval from dual
	
	</select>	
	
	
	<update id="insertBoard">
		insert into tbl_board (bnum, title, content, writer,pjnum,kind )
		values(#{bNum},#{title},#{content},#{writer},#{pjNum},2)
	</update>

	<select id="selectBoardBybNum" resultType="board">
		select *
		from
		tbl_board
		where bnum=#{bNum} 
	</select>
	
	<update id="updateBoard">
		update tbl_board
		set title=#{title}, content=#{content}
		where
		bnum=#{bNum} 
	</update>

	<update id="deleteBoard">
		delete
		from tbl_board
		where bnum=#{bNum} 
	</update>

	<update id="increaseViewCnt">
		update tbl_board set count=count+1
		where
		bnum=#{bNum} 
	</update>

	
	
	<select id="selectSearchBoardList" resultType="board">
		<![CDATA[
			select * 
			from tbl_board
			where bnum>0 
		]]>
		and pjnum = #{pjNum} and kind = 2
		
		order by bnum desc
	</select>
	<select id="selectSearchBoardCount" resultType="int">
		<![CDATA[
			select count(*)
			from tbl_board
			where bnum>0 
		]]>
		 and pjnum = #{pjNum} and kind = 2
		
	</select>


	<select id="selectBoardAll" resultType="board">
		select *
		from tbl_board
		order by bnum desc,inDate desc
	</select>
	</mapper>
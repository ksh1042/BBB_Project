<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
   
	<update id="insertBoard">
		insert into tbl_board(bnum,pjnum,title,content,writer,kind)
		values(bnum_seq.nextval,#{pjNum},#{title},#{content},#{writer},#{kind})
	</update>
	
	<update id="updateBoard">
		update
		tbl_board set
		title=#{title}, content=#{content}, updatedate=#{updateDate}
		where bnum=#{bNum}
	</update>
	
	<update id="deleteBoard">
		delete
		from tbl_board
		where bnum=#{bNum}
	</update>
	
	<update id="increaseCount">
		update tbl_board
		set count=count+1
		where bNum=#{bNum}
	</update>
	
	<sql id="search">
			<if test="searchType == 't'.toString()">
				and title like '%'||#{keyword}||'%'
			</if>
			<if test="searchType == 'w'.toString()">
				and writer like '%'||#{keyword}||'%'
			</if>
	</sql>
	<select id="selectSearchBoardList" resultType="board">
		<![CDATA[select *
		from tbl_board
		where bnum >0
		]]>
		<include refid="search" ></include>
		order by indate desc
	</select>
	<select id="selectSearchBoardCount" resultType="int">
		<![CDATA[
		select count(*)
		from tbl_board
		where bnum >0
		]]>
		<include refid="search"></include>
	</select>
	
	<select id="selectBoardByBum" resultType="board">
		select *
		from tbl_board
		where bnum=#{bNum}
	</select>
	
	<select id="selectBoardAll" resultType="board">
		select *
		from tbl_board
		order by bnum desc, indate desc		
	</select>
	  

</mapper>






   
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Question">
	
	<select id="getQnaSeqNextValue" resultType="int">
		select qanum_seq.nextval from dual
	</select>
	

	<update id="insertQna" >
		insert into tbl_question(qanum, title, content, writer)
		values(qanum_seq.nextval, #{title}, #{content}, #{writer})
	</update>
	
	<update id="updateQna">
		update tbl_question
		set title=#{title}, content=#{content}
		where qanum=#{qaNum}
	</update>
	
	<update id="deleteQna">
		delete 
		from tbl_question
		where qanum=#{qaNum}
	</update>
	
	<sql id="search">
		<if test="searchType == 't'.toString()">
			and title like '%'||#{keyword}||'%'
		</if>
		<if test="searchType == 'w'.toString()">
			and writer like '%'||#{keyword}||'%'
		</if>
		<if test="searchType == 'tw'.toString()">
			and (
			title like '%'||#{keyword}||'%'
			or
			writer like '%'||#{keyword}||'%'
			)
		</if>
	</sql>

	<select id="selectSearchQnaList" resultType="question">
		select * 
		from tbl_question
		where qanum is not null
		<include refid="search"></include>
		order by qanum desc
	</select>
	<select id="selectSearchQnaCount" resultType="int">
		select count(*)
		from tbl_question
		where qanum is not null
		<include refid="search"></include>
		order by qanum desc
	</select>
	
	
		
	<update id="increaseViewCnt">
		update tbl_question set viewcnt=viewcnt+1
		where
		qanum=#{qaNum}
	</update>
	
	<select id="selectQnaByQaNum" resultType="question">
		select *
		from tbl_question
		where qanum=#{qaNum}
	</select>
</mapper>

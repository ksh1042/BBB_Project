<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Issue">

	<select id="getBseqNextValue" resultType="int">
		select inum_seq.nextval from dual
	</select>

	<sql id="search">
		<if test="cri.searchType == 't'.toString()">
			and title like '%'||#{cri.keyword}||'%'
		</if>
		<if test="cri.searchType == 'w'.toString()">
			and writer like '%'||#{cri.keyword}||'%'
		</if>
		<if test="cri.searchType == 'tw'.toString()">
			and (
			title like '%'||#{cri.keyword}||'%'
			or
			writer like '%'||#{cri.keyword}||'%'
			)
		</if>
	</sql>
	
	<select id="selectSearchIssueList" resultType="issue">
		select * 
		from tbl_issue
		where inum is not null
		and pjnum=#{pjNum}
		<include refid="search"></include>
		order by inum desc
	</select>
	
	<select id="selectSearchIssueCount" resultType="int">
		select count(*)
		from tbl_issue
		where inum is not null
		and pjnum=#{pjNum}
		<include refid="search"></include>
		order by inum desc
	</select>
	
	<update id="insertIssue" >
		insert into tbl_issue(inum, title, content, writer,openyn,pjnum)
		values(inum_seq.nextval, #{title}, #{content}, #{writer},0,#{pjNum})
	</update>
	

</mapper>
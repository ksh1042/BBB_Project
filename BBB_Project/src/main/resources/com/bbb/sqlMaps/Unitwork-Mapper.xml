<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Unitwork">
	
	<select id="selectUnitworkListByRdNum" parameterType="int" resultType="unitwork">
		SELECT *
		FROM view_requirement_unitwork
		WHERE udNum=#{udNum}
	</select>
	<select id="selectUnitworkCountByRdNum" parameterType="int" resultType="int">
		SELECT count(*)
		FROM view_requirement_unitwork
		WHERE udNum=#{udNum}
	</select>
	
	<select id="selectUnitworkHistoryByRdNum" parameterType="int" resultType="unitworkHist">
		SELECT rownum, udNum, updateDate, comm
		FROM tbl_unitwork_definition_hist
		WHERE udNum=#{udNum}
		ORDER BY rownum DESC
	</select>
	<select id="selectUnitworkHistoryCountByRdNum" parameterType="int" resultType="int">
		SELECT count(*)
		FROM tbl_unitwork_definition_hist
		WHERE udNum=#{udNum}
	</select>
	
	
	<select id="selectUdNum" resultType="int">
		SELECT udNum_seq.nextval
		FROM dual
	</select>
	<select id="selectUddNum" resultType="int">
		SELECT uddNum_seq.nextval
		FROM dual
	</select>
	
	
	<update id="insertUD" parameterType="int">
		INSERT INTO tbl_unitwork_definition(udNum)
		VALUES(#{udNum})
	</update>
	
	<update id="deleteUDD" parameterType="int">
		DELETE FROM tbl_unitwork_definition_detail
		WHERE uddNum=#{uddNum}
	</update>
	
	<update id="updateUDD" parameterType="unitwork">
		UPDATE tbl_unitwork_definition_detail
		SET rddNum=#{rddNum}, udId=#{udId}, udName=#{udName}, udManager=#{udManager}, extDate=#{extDate}, intDate=#{intDate}, pdDate=#{pdDate}, devDate=#{devDate}, estmate=#{estmate}
		WHERE uddNum=#{uddNum}
	</update>
	<update id="insertUDD" parameterType="unitwork">
		INSERT INTO tbl_unitwork_definition_detail(uddNum, udNum, rddNum, udId, udName, udManager, extDate, intDate, pdDate, devDate, estmate)
		VALUES(#{uddNum}, #{udNum}, #{rddNum}, #{udId}, #{udName}, #{udManager}, #{extDate}, #{intDate}, #{pdDate}, #{devDate}, #{estmate})
	</update>
	
	
</mapper>






   